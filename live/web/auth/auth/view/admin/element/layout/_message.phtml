<?php if ($form && $element): ?>
    <?php $messages = $form->getMessagesFor($element); ?>

    <?php if (count($messages)): ?>
        <div class="has-error">
            <span class="help-block"><?php echo $messages[0]; ?></span>
        </div>
    <?php endif; ?>
<?php endif; ?>